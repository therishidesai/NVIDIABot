import socket

UDP_IP = "localhost"
UDP_PORT = 49152
MESSAGE = "Hello, World!!!"

sock = socket.socket(socket.AF_INET, socket.SOCK_DGRAM)

sock.sendto(bytes(MESSAGE, "utf-8"), (UDP_IP, UDP_PORT))
print "Message Sent to port: ", UDP_PORT
